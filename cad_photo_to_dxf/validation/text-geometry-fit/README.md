# P1B-3 full native TEXT geometry validation

Status: **P1B automated validation blocked**.

## Completed checks

- Full pytest command completed: `282 passed in 9.29s`.
- Formal real-document regression generated 12 document/DPI configurations covering 10 unique source pages.
- Every formal configuration failed against the committed expectations.

## Blocking result

The failures are dominated by a contract conflict rather than a newly discovered width-fit calculation change. The committed real-regression expectations still require candidates marked `replacement_unsafe` to be downgraded to fallback outlines. The approved current non-destructive editable-text contract explicitly removes `replacement_safe` as a native `TEXT` output gate.

Current outputs therefore contain the approved eligible editable `TEXT` objects, while the baselines expect substantially fewer editable objects and many fallback outlines. This changes editable/fallback counts, contour counts, content hashes and `structure_id` values on every formal page.

P1B-3 is not authorized to restore the superseded routing, modify production geometry, lower thresholds, or record replacement baselines. PR #28 must remain Draft and must not be merged.

## Checks not completed

The workflow stopped at the mandatory formal regression gate. Consequently the following checks have no P1B-3 result and must not be described as passed:

- Ruff;
- Python compile check;
- all-DXF read-save-read;
- actual generated-DXF `346 -> 0` clamp verification;
- new boundary crossing calculation;
- complete single-page versus batch entity equivalence rerun;
- approved before/after screenshots and LibreCAD UAT package.

The three representative pages were regenerated by the formal regression runner, but their formal comparisons failed before a releasable UAT package could be assembled.

## Evidence

- `validation-run.json`: commands, return codes and execution status.
- `formal-real-regression-failures.json`: per-configuration expected/observed evidence.
- `failure-analysis.md`: blocker analysis and required independent decision.
- `test-results.xml`: faithful machine-readable transcription of the successful pytest summary from Actions run `30631003176`, job `91157062020`.
- `manual-uat/NOT_READY.md`: explicit prohibition on treating current files as an approved UAT package.

No production code, OCR behavior, threshold, regression baseline, structure logic, color, PDF export behavior or `FinalStructure` behavior was changed in P1B-3. P2 was not started.
