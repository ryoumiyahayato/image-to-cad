# 审计问题逐项整改矩阵

审计基线：`e952de551dd0a01bc5d532240193ef8ee7dcb9f7`

整改分支：`fix/audit-remediation`

状态定义：

- **已实现，待独立验证**：修复代码和回归测试已进入分支，但必须以实际执行记录为准；
- **部分整改**：高风险路径已收敛，但问题的完整工程目标仍未实现；
- **未完成**：需要本地 Git、外部软件、真实数据或仓库管理权限。

| 审计项 | 当前状态 | 已采取措施 | 尚缺验收或后续工作 |
|---|---|---|---|
| 1. 发布工作流偏离源码且具有破坏性 | 已实现，待独立验证 | 删除旁路 Base64 源码发布；仅 tag 触发；构建精确提交；版本与 `app.__version__` 一致；不覆盖既有 Release；普通 CI 只读；安装器读取单一版本源 | 配置 `production-release` Environment 的人工审批；实际运行 Windows 构建和 tag 发布演练 |
| 2. 严格纸张识别接受非纸张对象 | 已实现，待独立验证 | 删除无条件 `minAreaRect` 回退；增加面积、凸性、矩形度、内部亮度、外部背景对比和纸张比例残差；严格模式使用明确置信度阈值；增加内部图框、圆形和空白负例 | 在真实手机照片负例集上验证误报率，并根据数据校准阈值 |
| 3. GUI 静默绕过透视校正 | 已实现，待独立验证 | 未确认透视时阻止预处理、识别和导出；低置信度自动候选必须人工确认；保存自动/手工角点、置信度和旋转记录 | Windows GUI 状态流和交互测试 |
| 4. 输出纸面尺寸被误解为设计尺寸 | 已实现，待独立验证 | 明确 `pixel`、`paper_mm`、`model_mm`；纸张校准只标记 `paper_mm`；已知尺寸校准才标记 `model_mm`；GUI、CLI、报告和 README 均提示 | 使用带已知原始 CAD 和设计尺寸的夹具验证模型尺度误差 |
| 5. 最终坐标修改后没有再次去重 | 已实现，待独立验证 | 在正交化和最终吸附后再次过滤非法/零长度实体、规范端点顺序并去重；合并来源和历史；报告最终重复数量；加入回归夹具 | 在仓库样例和真实夹具上确认完全重复实体为 0 |
| 6. 固定像素参数导致分辨率敏感 | 已实现，待独立验证 | 预处理、检线、吸附、桥接、共线和去重阈值按完整校正图长边缩放；独立几何工具才退回线段跨度；增加稀疏高分辨率夹具 | 对同一原图多个分辨率版本计算实体和几何差异 |
| 7. 粗线产生双边候选 | 部分整改 | 对高宽度候选执行保守的距离变换中心脊线校正，使同一笔画双边更易在后续去重中合并 | 仍不能自动判断墙体双边与单一粗线；需要真实标注数据和可交互中心线/轮廓策略 |
| 8. GUI 和 CLI 维护两套处理/报告逻辑 | 部分整改 | 活动 GUI 与 CLI 共用 `PipelineService` 和 `ReportBuilder`；GUI 的保护、复核和导出拆为独立模块 | 历史 `gui.py` 中仍有遗留实现，待测试稳定后删除；需要 GUI 回归测试确认无行为漂移 |
| 9. 测试不足以证明效果 | 部分整改 | 增加纸张负例、最终去重、GUI 门槛、分辨率、共享服务、拓扑、人工图层、圆形确认和 DXF 验证测试；加入 Ruff、mypy、compileall；定义真实夹具 manifest | GitHub Actions 当前未形成可用步骤日志；仍缺真实手机照片、ground truth、误差阈值和固定 FreeCAD 成功记录 |
| 10. 仓库污染严重 | 未完成 | 增加项目级忽略规则；删除危险临时清理工作流；明确索引清理和历史重写流程 | 仍需在本地 Git 中执行 `git rm --cached` 清理当前索引；经备份和维护窗口使用 `git filter-repo` 清理历史；协作者重新克隆 |

## 审计扩展项

| 扩展项 | 当前状态 | 说明 |
|---|---|---|
| 拓扑与闭合证据 | 已实现，待独立验证 | 交点分割、端点图、悬空端点、小间隙、连接分量、闭合/开放分量和未解析交叉进入统一报告 |
| 圆形实体 | 部分整改 | 只有达到置信度阈值并经 GUI 人工逐项确认的候选可导出为 DXF `CIRCLE`；导出器再次执行阈值门槛；圆弧仍未恢复 |
| 图层语义 | 部分整改 | 支持逐实体人工改层并保留自动分类理由和覆盖历史；仍不代表已恢复墙体、轴线或建筑语义 |
| 独立 DXF 验证 | 已实现，待独立验证 | `validation/validate_dxf.py` 生成格式、实体、重复、非法几何和拓扑 JSON 证据 |
| FreeCAD 独立导入 | 工具已实现，证据未完成 | `validation/freecad_import_check.py` 使用 FreeCAD `importDXF.open()` 并生成 JSON；尚无固定版本成功运行记录 |
| 真实工程精度 | 未完成 | 没有许可明确的真实照片和原始 CAD ground truth，不得宣称生产级 CAD 恢复或工程精度 |

## 合并门槛

本 PR 保持 Draft，至少满足以下条件后才能转为 Ready：

1. pytest、Ruff、核心 mypy 和 compileall 有可复核的成功记录；
2. Windows PyInstaller、便携版、安装器和卸载冒烟测试成功；
3. 仓库样例生成的 DXF 通过机器可读验证；
4. 固定 FreeCAD 版本导入成功并保存 JSON；
5. 当前 Git 索引不再跟踪生成目录和缓存；
6. 对最终补丁完成一次独立代码审查。

即使上述门槛全部满足，真实工程精度仍必须由带 ground truth 的数据集单独证明。
